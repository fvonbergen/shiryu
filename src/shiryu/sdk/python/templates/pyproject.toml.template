[project]
name = "{{project_name}}"
version = "0.1.0"
# We are using python3.9 with the | operator for dictioaries
requires-python = ">=3.12"
dependencies = []
dynamic = ["version"]

[build-system]
requires = ["hatchling==1.25.0", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"
